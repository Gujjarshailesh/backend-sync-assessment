# Application
NODE_ENV=development
PORT=3000

# Later phases will add further required variables here as they're introduced
# (DATABASE_URL, HUBSPOT_*, GOOGLE_*, STRIPE_* credentials, etc.) - keep this
# file in sync with src/config/env.validation.ts so `.env.example` always
# reflects exactly what the app needs to boot.
